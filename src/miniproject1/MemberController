public class MemberController {
    //회원가입 메서드
    final MemberService MemberService;
    //생성자 주입
    final public MemberController(MemberService MemberService){
        this.MemberService = MemberService;
    }


private String register(Member member){
    //1. 값 검증(아이디 중복 확인)
    if (MemberService.isExistMemberId(member.getMemid())){
        return "회원가입 실패: 아이디가 이미 존재하거나 오류가 발생했습니다."
    }
    //2. 아이디가 중복되지 않으면 회원가입한다.
    //3. 성공이면 성공메시지를 리턴한다.

    if (insertUser(member)){
        System.out.println("")
    }
   

}


}